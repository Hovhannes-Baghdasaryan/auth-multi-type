export enum E_AUTH_TYPE {
    NICKNAME = "nickname",
    EMAIL = "mail",
    PHONE = "phone",
}

export enum HttpStatus {
    NotFound = 404,
    Forbidden = 403,
    Internal = 500,
    BadRequest = 400
}